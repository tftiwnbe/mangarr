services:
  convex-dashboard:
    image: ghcr.io/get-convex/convex-dashboard:latest
    stop_grace_period: 10s
    stop_signal: SIGINT
    ports:
      - "6791:6791"
    environment:
      - NEXT_PUBLIC_DEPLOYMENT_URL=http://mangarr:3210
    depends_on:
      mangarr:
        condition: service_started

  mangarr:
    build:
      context: .
      dockerfile: Dockerfile
      target: mangarr-dev
    ports:
      - "3737:3737"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./web:/app/web
      - ./worker:/app/worker
      - web_node_modules:/app/web/node_modules
      - worker_node_modules:/app/worker/node_modules

volumes:
  web_node_modules:
  worker_node_modules:
