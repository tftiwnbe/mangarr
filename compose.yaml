services:
  mangarr:
    build:
      context: .
      dockerfile: Dockerfile
      target: mangarr-runtime
    ports:
      - "3737:3737"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
