services:
  mangarr:
    build:
      context: .
      dockerfile: Dockerfile
    image: mangarr:latest
    ports:
      - 3000:3000
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: unless-stopped
